<!DOCTYPE html>
<html>
<head>
  <%= erb :head, :locals => {
                    title: title,
                    author: author,
                    description: description,
                    keywords: keywords
                    } %>
</head>
<body>
  <%= erb :navbar %>
  <%= yield_content :title %>
  <%= yield_content :content %>
  <%= erb :footer, :locals => { author: author} %>
</body>
</html>
